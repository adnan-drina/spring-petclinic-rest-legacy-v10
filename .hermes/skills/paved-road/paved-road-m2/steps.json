{
  "kind": "m2-plan",
  "artifact": "m2-admission",
  "steps": [
    {
      "id": "assert-planner-activated",
      "backing": "native",
      "native": "assert-planner-activated.py",
      "note": "Activation gate (SAD v3 \u00a79). Refuses while .hermes/pins.json planner.activation is not-activated (or a pilot seal does not cover this bundle): kanban_block naming the gate; do not improvise a plan."
    },
    {
      "id": "bootstrap-destination",
      "backing": "skill",
      "skill": "bootstrap-destination",
      "keep": [
        "evidence/producers/bootstrap.json"
      ],
      "note": "Deterministic baseline from the compat-mapping catalog + pins (import, pom, properties, main class) and the decided effective datasource (decisions.yaml datasource, under an accepted ADR). No model. Then run scripts/check-datasource-decision.py: the destination's unprefixed datasource keys, its JDBC extension and its schema/seed assets must be the decided ones, because the platform resolves the datasource at BUILD time and a %profile-prefixed key alone is not a configured datasource."
    },
    {
      "id": "build-worklist",
      "backing": "skill",
      "skill": "build-worklist",
      "keep": [
        "evidence/planning/worklist.json",
        "verification/loop/steps.json"
      ],
      "note": "The only plan: JDK diagnostics + surefire + MTA rescan \u2192 clustered, ordered work list; records the baseline step."
    },
    {
      "id": "admit-migration-plan",
      "backing": "skill",
      "skill": "admit-migration-plan",
      "producer": true,
      "keep": [
        "evidence/planning/admission-receipt.json"
      ]
    },
    {
      "id": "k4-mint",
      "backing": "kernel",
      "kernel": "k4_mint.py",
      "note": "Mints exactly one card (the head cluster, or M4 VERIFY when the list is empty) from an ADMITTED receipt; zero commands otherwise. INCONCLUSIVE is a legal stop: kanban_block kind=needs_input naming the BLOCK classes."
    },
    {
      "id": "verify-live-kanban-loop",
      "backing": "skill",
      "skill": "verify-live-kanban-loop",
      "keep": [
        "evidence/receipts/k3/live-board.json"
      ]
    }
  ]
}
