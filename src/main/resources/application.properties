# active profiles config
#
# application use two active profiles
#
# one - for select database
# ------------------------------------------------
# When using HSQL, use: hsqldb
# When using MySQL, use: mysql
# When using PostgeSQL, use: postgresql
# ------------------------------------------------
#
# one for select repository layer
# ------------------------------------------------
# When using Spring jpa, use: jpa
# When using Spring JDBC, use: jdbc
# When using Spring Data JPA, use: spring-data-jpa
# ------------------------------------------------

# bootstrap: no Quarkus equivalent for spring.profiles.active (dropped)

# ------------------------------------------------

quarkus.http.port=9966
quarkus.http.root-path=/petclinic/

# Springfox workaround required by Spring Boot 2.6
# See https://github.com/springfox/springfox/issues/3462
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

spring.messages.basename=messages/messages
# bootstrap: no Quarkus equivalent for spring.jpa.open-in-view (dropped)

quarkus.log.category."org.springframework".level=DEBUG

#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# enable the desired authentication type
# by default the authentication is disabled
petclinic.security.enable=false

%hsqldb.spring.sql.init.schema-locations=classpath*:db/hsqldb/initDB.sql
%hsqldb.spring.sql.init.data-locations=classpath*:db/hsqldb/populateDB.sql
%hsqldb.spring.jpa.database=HSQL
%hsqldb.spring.jpa.database-platform=org.hibernate.dialect.HSQLDialect

%mysql.spring.jpa.database=MYSQL
%mysql.spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

%postgresql.spring.jpa.database=POSTGRESQL
%postgresql.spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# bootstrap: effective datasource decided in decisions.yaml (ADR-009); credentials are
# environment references, and the engine change from hsqldb is recorded in that ADR.
quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.url=${PETCLINIC_DB_URL}
quarkus.datasource.username=${PETCLINIC_DB_USER}
quarkus.datasource.password=${PETCLINIC_DB_PASSWORD}
quarkus.hibernate-orm.database.generation=none

# bootstrap[undecided-datasource]: the hsqldb, mysql, postgresql datasource families are not this destination's
# bootstrap[undecided-datasource]: configuration and this run never selects those profiles; the legacy
# bootstrap[undecided-datasource]: copy is preserved verbatim under .derived/frozen-input (ADR-009).

# bootstrap: the build profiles decided in decisions.yaml (ADR-011); the legacy
# selected implementations with spring.profiles.active=hsqldb,spring-data-jpa
quarkus.profile=prod,spring-data-jpa
# decided repair adr014-property-auth-basic (ADR-014)
quarkus.http.auth.basic=false
# decided repair adr014-property-auth-proactive (ADR-014)
quarkus.http.auth.proactive=false

# rhoai3:source-cors:begin -- harness capability source-cors-response-adapter/v1 (ADR-019), installed by restore-source-response-shape.
# Every value below is rendered from the evidence; do not edit by hand, re-run the installer.
quarkus.http.cors.enabled=true
quarkus.http.cors.origins=*
quarkus.http.cors.methods=DELETE,GET,HEAD,POST,PUT
quarkus.http.cors.headers=*
quarkus.http.cors.exposed-headers=content-type,errors
quarkus.http.cors.access-control-max-age=1800
quarkus.http.cors.access-control-allow-credentials=false
rhoai3.source-cors.preflight-authenticated-when=petclinic.security.enable=true
rhoai3.source-cors.rule-count=1
rhoai3.source-cors.rule.0.origins=*
rhoai3.source-cors.rule.0.headers=*
rhoai3.source-cors.rule.0.exposed-headers=errors, content-type
rhoai3.source-cors.rule.0.max-age=1800
rhoai3.source-cors.rule.0.mapping-count=34
rhoai3.source-cors.rule.0.mapping.0=* /
rhoai3.source-cors.rule.0.mapping.1=GET /api/owners
rhoai3.source-cors.rule.0.mapping.2=POST /api/owners
rhoai3.source-cors.rule.0.mapping.3=GET /api/owners/*/lastname/{lastName}
rhoai3.source-cors.rule.0.mapping.4=DELETE /api/owners/{ownerId}
rhoai3.source-cors.rule.0.mapping.5=GET /api/owners/{ownerId}
rhoai3.source-cors.rule.0.mapping.6=PUT /api/owners/{ownerId}
rhoai3.source-cors.rule.0.mapping.7=GET /api/pets
rhoai3.source-cors.rule.0.mapping.8=POST /api/pets
rhoai3.source-cors.rule.0.mapping.9=GET /api/pets/pettypes
rhoai3.source-cors.rule.0.mapping.10=DELETE /api/pets/{petId}
rhoai3.source-cors.rule.0.mapping.11=GET /api/pets/{petId}
rhoai3.source-cors.rule.0.mapping.12=PUT /api/pets/{petId}
rhoai3.source-cors.rule.0.mapping.13=GET /api/pettypes
rhoai3.source-cors.rule.0.mapping.14=POST /api/pettypes
rhoai3.source-cors.rule.0.mapping.15=DELETE /api/pettypes/{petTypeId}
rhoai3.source-cors.rule.0.mapping.16=GET /api/pettypes/{petTypeId}
rhoai3.source-cors.rule.0.mapping.17=PUT /api/pettypes/{petTypeId}
rhoai3.source-cors.rule.0.mapping.18=GET /api/specialties
rhoai3.source-cors.rule.0.mapping.19=POST /api/specialties
rhoai3.source-cors.rule.0.mapping.20=DELETE /api/specialties/{specialtyId}
rhoai3.source-cors.rule.0.mapping.21=GET /api/specialties/{specialtyId}
rhoai3.source-cors.rule.0.mapping.22=PUT /api/specialties/{specialtyId}
rhoai3.source-cors.rule.0.mapping.23=POST /api/users
rhoai3.source-cors.rule.0.mapping.24=GET /api/vets
rhoai3.source-cors.rule.0.mapping.25=POST /api/vets
rhoai3.source-cors.rule.0.mapping.26=DELETE /api/vets/{vetId}
rhoai3.source-cors.rule.0.mapping.27=GET /api/vets/{vetId}
rhoai3.source-cors.rule.0.mapping.28=PUT /api/vets/{vetId}
rhoai3.source-cors.rule.0.mapping.29=GET /api/visits
rhoai3.source-cors.rule.0.mapping.30=POST /api/visits
rhoai3.source-cors.rule.0.mapping.31=DELETE /api/visits/{visitId}
rhoai3.source-cors.rule.0.mapping.32=GET /api/visits/{visitId}
rhoai3.source-cors.rule.0.mapping.33=PUT /api/visits/{visitId}
# rhoai3:source-cors:end

# Preserve the source documentation redirect with the packaged Swagger UI.
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/petclinic/swagger-ui/index.html

# rhoai3:source-media-type:begin -- harness capability source-media-type-parameter-adapter/v1 (ADR-019), installed by restore-source-response-shape.
# Every value below is rendered from the evidence; do not edit by hand, re-run the installer.
rhoai3.source-media-type.parameter=charset
rhoai3.source-media-type.parameter-value=UTF-8
rhoai3.source-media-type.media-types=application/json
# rhoai3:source-media-type:end
